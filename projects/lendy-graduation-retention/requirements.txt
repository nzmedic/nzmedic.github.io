
pandas>=2.0
scikit-learn>=1.3,<1.4.2
scipy>=1.10
joblib>=1.3,<1.5
tqdm>=4.66,<4.7

# Optional explainability: if installed, pipeline will use it; otherwise fallback methods run.
shap==0.49.1

# Core numerical stack (pin away from NumPy 2.x surprises)
numpy>=1.26,<2.0
scipy>=1.10,<1.13

# Data
pandas>=2.2.2,<2.3  # 2.2.2+ is generally compatible with NumPy 2.0, but we stay on NumPy 1.x for stability

# Explainability
shap>=0.49.1,<0.50  # v0.50.0+ requires Python 3.11+
numba<0.60        # known break reports with shap 0.44 + numba 0.60.0

# SHAP runtime deps. Explicit to control for dep risk
cloudpickle>=2.0,<4.0
packaging>=23,<26
slicer>=0.0.7,<0.1
